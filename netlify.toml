#[[plugins]]
#  package = "netlify-plugin-lunr-indexer"
#    [plugins.inputs]
#    jsonPath = "dist/contents/" # Chemin vers vos données JSON
#    indexPath = "dist/index/full-search.json" # Chemin de sortie de l'index

[[headers]]
  for = "/*"
  [header.values]
    X-Frame-Options= "ALLOWALL"
  
  for = "/.netlify/functions/*"  # Adjust the path according to your function
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"

[build]
  functions = "./functions"
  command = "npm i"